{
    Filter = {
        Bundles = (
            "com.stone.solo.cn",
            "com.tencent.xin",
            "com.chaitin.baizhi.monkeycode",
        );
    };
    DKConfig = {
        // 需要多账号支持的应用 Bundle ID 列表
        // 添加新应用只需在此数组中追加对应的 Bundle ID
        SupportedBundles = (
            "com.stone.solo.cn",
            "com.tencent.xin",
            "com.chaitin.baizhi.monkeycode",
        );
        // 悬浮按钮隐藏方式: "doubletap"(双击) / "swipe"(侧滑) / "both"(两者)
        HideGesture = "both";
        // 三指长按触发时间 (秒)
        LongPressDuration = 1.0;
        // 是否启用推送通知桥接
        EnablePushBridge = true;
        // 心跳保活间隔 (秒)
        HeartbeatInterval = 1800;
    };
}